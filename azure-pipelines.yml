trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:

- task: Docker@2
  displayName: Login to DockerHub
  inputs:
    command: login
    containerRegistry: Azure_DevOps-to-DockerHub

- script: |
    echo "Docker login successful"
    docker info
  displayName: Verify Docker login
